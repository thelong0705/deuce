*

# The build stages copy what they need; everything else stays out of the
# context so an image is not rebuilt because the frontend or a doc changed.
!go.mod
!go.sum
!cmd
!internal
!api

# Dockerfile.migrate bakes these in.
!db/postgres/migration
